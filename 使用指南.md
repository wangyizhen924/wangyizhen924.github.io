# 网站使用指南

> 这份说明是给零编程基础的人看的。遇到不懂的地方，直接来问我。

---

## 一、先搞懂一件事：你的网站是怎么跑起来的

整个流程只有 3 个角色：

| 角色 | 是什么 | 在哪 |
| --- | --- | --- |
| 源文件 | 你现在电脑上这个文件夹 | `D:\南京大学\计算传播学导论\academic page\website` |
| 云端仓库 | 源文件在 GitHub 上的备份 | https://github.com/WangYizhen924/wangyizhen924.github.io |
| 服务器 | GitHub Pages，免费帮我们把源文件变成网页 | https://wangyizhen924.github.io |

真正的运转逻辑是：

```
你修改本地文件  →  提交(commit)  →  上传(push)  →  GitHub 自动编译  →  网站更新
                                                      （约 1 分钟）
```

**关键点：本地文件改了不会自动生效，必须 push 到 GitHub 才会更新网站。**

网站不用数据库、不用服务器后台，全部是一堆纯文本文件（Markdown / YAML）。所以你"改内容"的本质就是改文本。

---

## 二、改哪里？文件对照表

| 想改什么 | 打开这个文件 |
| --- | --- |
| 网站标题、你的姓名、侧边栏简介、邮箱、社交链接 | `_config.yml` |
| 顶部导航栏的菜单顺序和名称 | `_data/navigation.yml` |
| 首页（"关于我"） | `_pages/about.md` |
| 简历页面 | `_pages/cv.md` |
| 论文发表列表 | `_publications/` 目录，新建 `.md` 文件 |
| 学术报告 | `_talks/` 目录 |
| 教学经历 | `_teaching/` 目录 |
| 博客文章 | `_posts/` 目录 |
| 头像图片 | `images/profile.png` |
| 404 页面文案 | `_pages/404.md` |

**不要动**的目录（动了容易把网站搞坏）：`_sass`、`_includes`、`_layouts`、`assets`。
这些是"装修"，不是"内容"。

---

## 三、常见操作怎么做

### 1. 改名字、简介、邮箱

打开 `_config.yml`，找到这几行，把 `【】` 里的内容换成你自己的：

```yaml
title       : "王小明 的个人主页"
name        : &name "王小明"
description : &description "王小明的个人学术主页"

author:
  name     : "王小明"
  bio      : "南京大学新闻传播学院本科生，关注社交媒体与公共舆论。"
  location : "南京"
  employer : "南京大学"
  email    : "xiaoming@example.com"
```

> ⚠️ 两个最容易踩的坑：
> 1. **冒号后面必须有一个空格**（`name:` 加空格再加内容），少一个空格网站就可能构建失败。
> 2. 内容要用**英文的双引号 `"`** 包起来，不要用中文的 `“”`。

### 2. 换头像

1. 准备一张正方形照片，至少 400×400 像素
2. 把文件重命名为 `profile.png`
3. 覆盖掉 `images/profile.png` 这个文件

### 3. 写一篇博客

在 `_posts/` 目录新建一个文件，**文件名必须是 `年-月-日-英文短标题.md`** 的格式，例如：

```
_posts/2026-09-20-my-first-post.md
```

文件内容这样写：

```markdown
---
title: "我的第一篇博客"
date: 2026-09-20
tags:
  - 课程笔记
  - 计算传播
---

这里是正文。可以直接用 Markdown 语法写作。

## 二级标题

- 列表项一
- 列表项二
```

注意：最上面被两条 `---` 夹住的部分叫 **front matter**，是给网站程序看的"元数据"，必须放在文件最开头。

### 4. 加一篇论文

在 `_publications/` 目录新建 `2026-09-20-论文标题.md`：

```markdown
---
title: "论文标题"
collection: publications
category: conferences      # 可选 conferences（会议论文）/ manuscripts（期刊论文）/ books（专著）
permalink: /publication/2026-09-20-论文标题
excerpt: '一句话摘要'
date: 2026-09-20
venue: '期刊或会议名称'
paperurl: '/files/论文全文.pdf'    # 把 PDF 放进 files/ 目录即可
citation: '王小明. (2026). &quot;论文标题.&quot; <i>期刊名</i>.'
---

这里写论文的详细说明。
```

### 5. 增删顶部导航菜单

打开 `_data/navigation.yml`，每个菜单项就是三行：

```yaml
  - title: "论文发表"
    url: /publications/
```

- **想删掉某个菜单**：把这三行整块删掉即可（页面文件还在，随时能加回来）
- **想调整顺序**：上下挪动这些块
- **想加回作品集**：加上
  ```yaml
  - title: "作品集"
    url: /portfolio/
  ```

---

## 四、Markdown 最常用语法速查

| 写法 | 效果 |
| --- | --- |
| `# 一级标题` | 大标题 |
| `## 二级标题` | 中标题 |
| `**加粗**` | **加粗** |
| `*斜体*` | *斜体* |
| `- 项目` | 无序列表 |
| `1. 项目` | 有序列表 |
| `[文字](https://链接)` | 超链接 |
| `![说明](/images/图片.png)` | 插入图片 |
| `> 引用` | 引用块 |

小技巧：Markdown 里**段落之间要空一行**，否则会被连成一段。

---

## 五、改完怎么上线

在项目文件夹里打开终端（Git Bash），依次执行：

```bash
git add .
git commit -m "更新个人主页内容"
git push
```

推完之后等 1 分钟左右，刷新 https://wangyizhen924.github.io 就能看到变化。

---

## 六、出问题了怎么办

1. **网站没更新 / 打不开**
   打开 https://github.com/WangYizhen924/wangyizhen924.github.io/actions
   看有没有红色的 ❌。红色的那次点进去，最下面通常是具体报错位置。

2. **八成是 YAML 语法错误**
   检查 `_config.yml` 和各个 `.md` 文件顶部的 front matter：
   - 冒号后面有没有空格？
   - 引号是不是英文的 `"`？
   - 缩进是不是对齐了（要用空格，不能用 Tab）？

3. **改坏了想反悔**
   因为所有历史版本 GitHub 都存着，可以回到仓库的 `Commits` 页面，找到没坏的那一次，把文件内容复制回来覆盖。

4. **实在搞不定**
   直接把报错信息发给我，我帮你看。
